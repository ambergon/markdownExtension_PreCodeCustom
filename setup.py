from setuptools import setup, find_packages

setup(
    name='markdownExtension_PreCodeCustom',
    version="0.0.1",
    description="<pre><code key=value> to <pre key=value><code>",
    author='ambergon',
    packages=find_packages()
)
